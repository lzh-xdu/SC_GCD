"""SystemC web viewer package (stdlib only).

Renders a self-contained HTML page that shows, for a set of SystemC sources:
  - the module / port / connection structure parsed from the code, and
  - the simulation cycles animated from a real event CSV trace.
"""
