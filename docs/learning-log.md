# 学习与理解记录

## L-014：从代码追踪握手发布与零延迟交付

- 2026-09-08，用户追问代码依据，并提出“一个周期内完成直接deliver”的理解。
- Transform 在第3沿把旧 magnitude 槽移入 ordered 槽，再调用 valid/data.write；信号在该时间点的 update 阶段生效。Compute 仅敏感于 posedge，所以不会因 valid 改变再次运行，第4沿才通过 valid&&ready 调用 accept；与同沿两个进程的执行先后无关。
- b=0 不进入 gcdAndLatency 的 while，latency保持0；accept 直接记录complete、设 RESULT_PENDING 并调用deliver。准确表述为“接收沿同一仿真时间完成”，没有推进一个周期。
- deliver 只是尝试nb_write，FIFO满则保持结果待交付；成功后下游也只能在后续上升沿读取。普通C++函数执行耗费主机时间，但不自行推进SystemC仿真时间；非零任务也先算数值，再由后续tick倒计时表达规定延迟。
- 依据当前 src/stage2/transform.cpp、compute.cpp；仅代码讲解，未修改模型或重跑测试。用户理解尚待后续验证。

## L-013：传输少一拍、零计算延迟与等待归因

- 2026-09-07，用户提问；AI结合已验证时间线解释：阶段2直接握手省去中间FIFO的可见性等待，GCD计算仍6拍；不保证所有工作负载总周期都少1。
- 零延迟仅指排序后b=0、无取余，Compute接收沿即可完成；整个系统仍有变换/传输，非零相等输入需1拍。
- 任务延迟采用入模到输出的四段相加；均值可加，P95/最大值不可加。阻塞周期跨模块重叠，空闲也可能由排空或下游引起。
- 解释与实测见[统计说明](task-statistics.md)。这些是本轮教学内容，用户尚未独立回答，不记为理解验收通过。

## L-012：优化需要区分瓶颈、容量与主机开销

- 2026-09-07用户请求全面优化分析；AI说明模拟周期改善、主机执行变快和资源减少属于不同目标。
- 本轮零延迟FIFO探测说明深度1可能因delta后释放空位出现隔拍传递；当前默认2已避开，不能误报为默认实现缺陷。
- W4/8/16收益依赖负载；本次mixed在W16达到193周期，而random_01从W4开始不再改善。R1突发38累计结果等待仅使总周期多2，不能将并行/重叠等待直接当总延迟。
- 提醒用户：完成沿约束不可移除；RESULT_PENDING交付沿也禁止接收则是当前更严格的实现约定，可作为独立候选优化。用户尚未作出新方案选择。

## L-011：用户提出择闲调度与滑动窗口

- 用户提出空闲单元优先、双空闲随机、后续短任务先算先存、按编号输出，识别了轮转的空闲算力浪费。
- 任务id和Compute空闲/忙/结果待取出本来已存在；本轮主要改变派发和结果合并。
- 细化满窗口边界：若后续结果占满空间后连最早结果也拒收，可能循环等待。本实现提前为已派发任务预留槽，窗口满时停止新派发，继续收预留结果。
- W=8首项长场景在第78拍前完成7个短任务，旧版只有1个，但最终总周期120→121；提前计算不保证最终输出更快，还需看保序出口带宽和新增存储延迟。
- 实际偏斜/突发场景明显改善，连续斐波那契略退化；不能把优化视为所有输入都加速。说明与证据见stage3-window.md。

## L-010：背压计数不是额外总执行周期

- 用户正确指出FIFO也会阻塞上游，并质疑显式握手未产生额外性能影响。
- 澄清：FIFO隐含流控、握手显式流控；在同一瓶颈限制下可以有相同吞吐。缓冲可容纳更多等待任务，不能提高串行Compute固有服务能力。
- valid&&!ready只说明发送端有任务未获接收，可能与Compute处理旧任务重叠；不可把该计数加到计算忙周期上估算总周期，也不能把阻塞率当作吞吐损失百分比。
- 当前基本输入作业2有53阻塞周期、66忙周期、总78周期；作业1为79周期，一拍差源于移除中间FIFO。前述报告没有强调这种因果区别，本轮补充。
- 同40条(30,30)、D=1，作业2输出周期1→13使总周期84→520，结果等待0→425，握手阻塞0→451；作业1慢输出同样520。真实对照见evidence/stage2-blocking。
- 高阻塞并非总是坏事：当Compute仍高利用率地计算，背压是供给超过服务能力的表现；当结果等待高、有效计算利用率降低，应检查下游瓶颈。用户尚未进一步复述确认。

## L-009：用户验收回答与待补概念

- 用户正确指出上升沿接收、满时停止及逆序避免新旧覆盖；仍需补充两级内部槽也必须保持，逆序还防止新任务同沿穿透两级。
- 用户明确不知道完成沿限制：它是题目时序约束，不是 C++ 限制；k 完成旧任务，最早 k+1 接收下一条，不增加额外计算也不提前吞吐。
- 用户将零和 INT32_MIN 理解为“输入有效性”：二者本身均合法，重点是 gcd(a,0)/gcd(0,0) 的约定和 INT32_MIN 取绝对值前扩宽，结果2147483648不能放回有符号32位。
- 用户知道统计用于优化，但未给出分母/采样：明确总周期含启动排空，利用率忙周期除以单元数×总周期；沿后delta采样存储占用，握手按沿前值。
- 用户将参考/时序概括为验证正确：数值正确不代表完成时间正确，独立math.gcd检查值，事件检查延迟、顺序和吞吐；两者分别验收。
- 新增Compute意味着更多资源，但两倍单元不等于两倍速度/芯片面积/功耗。当前只能量化资源代理，真实PPA另需实现标定。上述为AI补充，用户尚未再次复述确认。

## L-008：长计算、随机测试与统计分离

- 用户主动提出边界、连续相邻斐波那契和短长混合场景，覆盖正常样例之外的压力路径。
- 补充说明：相邻斐波那契会产生较多 Euclidean 取余；本题按位宽差累加周期，不能把取余次数直接当总延迟，也不未经证明宣称加权最坏情况。
- 随机测试保留种子和实际输入以复现，功能参考使用 math.gcd；边界定向测试不可被均匀随机替代。
- 功能结果、性能指标、事件记录有不同用途。Compute 忙周期/模拟总周期是被建模硬件的利用率，不等于主机仿真速度。
- 本轮实例结果见 stage1-test-matrix.md；这些是 AI 说明与已执行测试，不宣称用户已独立掌握新增测试代码。

## L-007：显式握手不是给 FIFO 增加 ready 条件

- 用户提出 `if (!m_computReady || !m_tasksIn.nb_read(task))`；表达下游不准备时不取数据。
- 澄清：短路语义正确，但实际边界仍为 FIFO，尚无显式 valid/data 信号；题目要求接收由同沿 valid && ready 决定。
- 区分 Compute 接收分支和整个 tick：忙碌时还需推进计数，不能在 tick 顶部因 ready=false 提前返回。
- 接口方案与缓冲变化的注意事项见 stage2-interface-proposal.md；作业 2 尚未实现。

## L-006：命名空间与流水更新顺序

- 2026-09-06 用户指出 m_stage1/m_stage2 含义不清，询问 namespace stage1，并推断逆序推进用于避免同沿新值覆盖旧值。
- 解释：namespace stage1 是“作业 1 版本”的 C++ 名字分组，不是 SystemC 模块或第一流水级，不产生硬件资源。
- 本轮成员改为 m_magnitudeStage（绝对值级）与 m_orderedStage（排序级），保留 namespace 名称。
- 逆序推进的关键：optional/普通成员立即更新，不具备 sc_signal 的延迟提交；先发旧排序级，再移旧绝对值级，最后接新数据，防止同沿穿透两级，同时利用本沿释放的内部寄存器槽。
- 避免覆盖还依赖空位检查；只改变顺序而不检查容量仍不正确。外部 sc_fifo 仍不允许利用同沿新释放空位，勿与内部槽混淆。
- 另一实现选择是 current/next 双份状态；未在本轮引入，逆序代码并非硬件物理倒序执行。

## L-005：观察仿真与 sc_stop 提示

- 2026-09-06 用户询问如何看仿真过程，并报告 Simulation stopped by user 信息。
- 解释见 inspect-simulation.md：阶段 1 事件 CSV、性能汇总、断点，与 Day01 VCD 区分。
- 代码和库源码确认提示来自 sc_stop 的正常结束流程；不是单凭消息就认定用户手动中断或功能失败。
- 强调 compute_accept 中提前算出的 value 只是模型数值预测，完成时刻仍由 compute_complete 定义。

## L-004：用户独立阅读 Parser

- 2026-09-06 用户明确反馈：tick 中 num_free 判断决定满时停止，构造函数 sensitive << clk.pos() 决定每拍读取。
- 已观察到的理解：能够将背压与时钟触发对应到实际代码。
- AI 精确补充：上升沿敏感决定执行时机；每次 tick 只有一次 getline 和至多一次 nb_write、没有循环批量读取，三者共同保证每拍最多一条。仅敏感列表本身不能限制一次调用读取多条。
- 本轮代码依规范改为 m_tasksOut/m_clk，方法 tick 不变。尚未把理解一模块记成全部作业已掌握。

## L-004b：从 C++ 到 SystemC，再到电路

原编号 L-004；2026-09-06 审核消除重复编号，内容保留。

- 2026-09-06 用户要求面向三年 C++ 经验、已知时钟和信号概念的初学者进行完整讲解；以下为 AI 教学结论，尚无用户掌握情况的验证。
- SystemC 通过 C++ 类型、模块/端口/通道、进程和离散事件内核表达硬件结构与并发；普通编译生成主机仿真程序。SC_THREAD 是可挂起的仿真进程，不等于 GPU 执行线程或必须并行运行的操作系统线程。
- 普通赋值立即生效；sc_signal 写入在更新阶段生效。同一模拟时刻可以有多轮 delta 调度，不能把 delta 当成物理门延迟，也不能认为任意组合链等待一个 delta 就全部稳定。
- 信号本身不必对应寄存器：组合敏感进程与时钟触发进程表达不同硬件；位宽、跨周期状态和读写行为决定硬件含义。复位行为需要明确描述，仿真默认初值不能替代真实复位。
- 功能模型、周期模型和 TLM 选择不同抽象层次。架构模型可用主机立即计算结果再安排未来完成，但需要单独建模容量、占用、接收间隔和反压；模型设定的延迟不是电路实测结果。
- 延续 D-006：模型可以作为人工 RTL 实现的规格/参考，也可以在符合指定工具可综合子集和约束时，经 HLS 生成 RTL。HLS 再经逻辑综合与物理实现才到芯片版图；FPGA 则映射已有资源并生成配置位流。仿真可执行文件、测试平台和 VCD 不会直接变成电路。
- HLS 需要调度运算、分配/共享资源、保存跨周期状态并生成控制逻辑。延迟与启动间隔 II 是不同指标；循环次数、C++ 语句数和 wait(ns) 均不能直接当作硬件周期数或物理实现保证。
- 核对依据：本地 SystemC 3.0.1 内核与信号源码、现有 day01 示例；[Accellera 综合子集说明](https://www.accellera.org/activities/working-groups/systemc-synthesis)、[Siemens Catapult](https://www.siemens.com/en-us/products/ic/catapult-high-level-synthesis/hls/c-cplus/)、[HLS 架构与调度手册](https://docs.altera.com/r/docs/683152/current)、[OpenROAD 实现流程](https://openroad-flow-scripts.readthedocs.io/en/latest/user/UserGuide.html)。
- 本轮只做概念讲解和记录，未修改模型、未运行新测试或综合，也未提前实现任何作业阶段。

## L-003：时钟、波形与生命周期接口

- 2026-09-06 用户选择八处调用要求详细解释；说明见 [接口学习说明](day01-api-guide.md)。
- 核对本地 SystemC 源码与已存在 VCD：区分时钟周期/占空比/首沿时间、波形时间标尺/内核时间分辨率、sc_start 区间返回/sc_stop 结束阶段。
- 明确 dont_initialize 不是复位，sc_end_of_simulation_invoked 也不是功能正确性的证明。
- 本轮没有用户回答证据，不能记为已掌握。

## L-002：命名审查与综合边界

- 2026-09-06 用户主动发现端口名与方向不一致，提出连接信号按来源/去向命名；已落实 D-005。
- 用户提出 LLVM 与生成硬件的联系；本轮核对并解释普通 C++ 仿真编译与 SystemC 可综合子集/HLS 的区别。尚未观察到用户对该解释的反馈。

## L-001：SystemC 基础与流控

记录日期：2026-09-06。来源：环境搭建之后至本次请求之前的连续对话；不逐条断言原始消息的日历日期。

| 主题 | 对话证据与修正 | 状态 |
|---|---|---|
| 端口和信号 | 用户询问两个 `.input(input)`；解释为两个端口通过 operator() 绑定同一信号，tb 端是 sc_out，model 端是 sc_in；用户表示理解 | 口头确认，尚无独立代码练习 |
| 同沿旧值 | 用户正确回答第二沿更新后 stage1=20、output=10 | 有具体推演证据 |
| delta cycle | 用户能判断去掉零时间等待会检查失败；AI 纠正“下一步计算”为“本沿信号更新尚未生效” | 已讲解，仍需实际实验 |
| 同沿输入写入 | 用户判断寄存器仍采到 10；AI 补充 input 会更新成 20，stage1 不因 input 变化而再触发 | 已完成一次推演 |
| 有效数据 | 用户指出两个相同的 30 仍可能是两条任务，需要独立有效标记 | 有具体回答证据 |
| ready 含义 | 用户先把 ready=0 与产生输出联系；AI 澄清只反映能否接收，不能据此判断完成/输出；用户表示理解 | 口头确认 |
| 结果保持 | 用户指出输出有效应保持、覆盖风险需流控；AI 补充结果待交付阶段也要限制接收 | 有具体回答，设计未实施 |

## 后续练习

- 独立修改最小示例的输入与预期，运行并解释失败或成功。
- 给出一次阻塞传输的逐沿表，明确何时只有等待、何时真正接收。
- 明确题目两周期流水的计时起止点，并与示例区别开。

这里记录的是已观察到的学习表现，不等于已经理解全部 SystemC 或完成作业阶段。

## L-006b：用 trace 区分驻留、计算与主机运行效率

原编号 L-006；2026-09-06 审核消除重复编号，内容保留。

- 2026-09-06，来源：用户请求终端图形化；以下为 AI 本轮解释，不代表用户已独立掌握。
- 半开区间显示沿后状态；FIFO 驻留包含下一沿可读的基础延迟，不能全部视为可消除的阻塞。
- Transform 驻留超过两周期可提示背压，但现有事件不能还原两寄存器内部每次移动。
- 模型利用率、吞吐与主机实际仿真速度是不同指标；当前 CSV 无主机耗时，不能推断事件驱动加速比。
- 图例、操作及本次观察见 [终端查看器](trace-viewer.md)。

## L-007b：VCD 与实际波形末尾的区别

原编号 L-007；2026-09-06 审核消除重复编号，内容保留。

- 2026-09-06，用户询问 Day01 VCD 的含义与查看方式；AI 实读四个信号和 0～20 ns 变化，给出 GTKWave 官方操作说明。
- VCD 记录信号变化，CSV 记录任务事件；当前 Python 查看器不能直接读取 VCD。
- 当前文件以裸 #25 结束，时间戳存在不代表该沿信号已记录；代码预期与文件证据必须分开。5 ns 采样至 15 ns 输出经过一个时钟周期。
- 详见 [实读记录](inspect-simulation.md#day01-vcd-实读与查看2026-09-06)。未验证用户理解程度，未修改模型。

## 2026-09-07：断言、异常与校验时机

- 用户提出 assert(condition, true) 用于压缩入口抛异常代码；C++17 标准 assert 只有一个条件参数，失败终止进程，NDEBUG 下可消除检查。
- 运行时输入/环境错误采用始终生效的 requireCondition；message 给出诊断上下文，异常交由调用边界处理。
- 入口只能检查已有信息；文件打开、读取和刷新仍需操作后检查。FIFO 满、EOF、空闲是协议状态，不是异常。
- 所有函数应审阅前置条件，但合法全取值域的纯函数/查询无需恒真断言；借用对象寿命也不能通过指针非空证明。
- 本轮属于 AI 解释与实现，尚未验证用户独立理解。例子与验证见 [详细记录](code-contracts.md)。
