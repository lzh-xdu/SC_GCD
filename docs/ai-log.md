# AI 协作记录

## A-018：保序成本分析与归因边界

- 2026-09-08，用户要求分析作业3保序的性能及资源影响；AI核对当前代码，复用历史容量及架构比较，未修改模型或新跑实验。
- 明确历史结果、代码推论和待实施对照，避免将择闲收益当纯保序收益、载荷bit当面积、阻塞计数当损失周期；详见[分析](stage3-ordering-impact.md)。用户尚未选择新实验或优化。

## A-017：收敛设计与实现统一统计

- 用户要求简洁记录全作业约束/优化目标，解释少一拍及零延迟，并补关键阻塞原因和任务延迟。
- AI新增最终设计入口、公共被动统计器、空闲与窗口阻塞子项、跨四模型独立事件校验；保留现有性能/输出语义。
- 一次性前后对照脚本首次误扫非法输入目录，读取不存在的events.csv失败；改为要求双方事件文件存在后，96组对照全部通过。该辅助错误及处理见验证记录，未虚构硬件缺陷。
- Debug/Release各6/6，原行为96组不变；用户尚未反馈新口径的理解，未把AI实现记为用户手改。关联D-017、L-013及[证据](evidence/task-statistics/validation.md)。

## A-016：作业1～3全面优化审查

- 2026-09-07，用户要求分析优化空间与方法；AI未修改现有模型、默认参数或随机策略。
- 读取源码/本地sc_fifo实现、现有统计，新增58组容量探测并独立核对GCD，区分已实测参数收益、未实施架构候选及未测主机微优化。
- 发现W8不是所有负载的平台，mixed可由257降至193（W16）；D1通道气泡、R1零延迟突发等待也有实际证据。保留无收益配置，不泛化为全局最优。
- 提出窗口头部旁路、关键阻塞归因、公共运行辅助等候选，明确题目约束和验收方法；没有假装这些建议已实现，详见stages1-3-optimization-review.md。

## A-015：按用户方案优化作业 3 并先保留旧基线

- 用户要求先跑旧版、再改择闲随机分配及滑动窗口，AI先运行32次旧Release并保存摘要/原始数据，随后新增独立模型。
- AI增加预留credit避免满窗口拒收最早结果的潜在死锁，保留单拍收一项的合并带宽；明确比仅限制完成结果更保守，没有隐藏窗口资源增加。
- 原编号/Compute状态复用，种子可重放；新方案68次性能运行、旧R=6等载荷容量对照与所有退化结果公开。
- 测试假设错误和修正见B-008；未声称死锁已在旧实现发生。用户判断来源与AI细化分开记录，代码由AI执行。

## A-014：补足握手阻塞指标的因果解释

- 用户提出FIFO已有背压、握手没有观察到额外性能损失的合理疑问。
- AI承认先前列出阻塞计数不足以说明因果；区分发送端等待、有效计算和结果等待，说明重叠周期不可相加。
- 不更改模型或人为加延迟来制造协议开销；用相同输入和D=1比较快/慢Output及FIFO版慢Output，保存实际输出与统计。见L-010和evidence/stage2-blocking。

## A-013：作业 3 严格轮转、主动缺陷与成本比较

- 按用户选择保持轮流分配/顺序处理，新建Dispatcher/Collector，共用作业2的Transform和Compute；用户未要求动态择闲。
- AI以长任务+短突发+慢输出主动检查最小结果FIFO，实际发现R=1的性能容量缺陷，记录R=1/2/4输入、观察、定位和配置改进，见B-006；没有故意制造功能错误。
- 独立随机负载约1.96倍加速，偏斜负载仅1.02倍；如实保留严格轮转的不足，不为了好看换输入隐藏结果。
- 用户成本问题落实为单元/槽/载荷容量和每Compute相对效率；不生成无依据的面积或功耗。
- 新增测试时误缩进Python循环，原始失败与修复见B-007。最终Debug全5/5，Release相关4/4加独立目录作业1通过。
- 用户发“继续任务”，延续原目标；没有因此推进作业4。新增代码由AI编写，未虚构候选人理解验收。

## A-012：用户授权作业 2/3 与资源指标

- 用户请求完成作业2、3，指定简单顺序分配和结果处理，提出额外计算单元的成本/面积/功耗问题。
- AI 将顺序明确为严格轮转0/1，成功传输后才换单元；先完成作业2验收与提交，再实现作业3。
- 作业2复用 Parser/Output，Transform排序槽直接握手，Compute ready表示真实空闲；追加沿前握手日志和独立回归。主要周期约定见 stage2-design.md。
- 指标选择：单元数、槽数和声明载荷容量作为代理，不伪造面积/功耗；用户没有提供工艺、频率或功耗标定。
- Debug 3/3；Release 新阶段通过，原阶段汇总文件 PermissionError 在独立目录复测通过；证据与限制见 stage2-run.md。补丁曾因格式化后的上下文变化未匹配，读取后重新应用，未导致源码错误。

## A-011：用户要求先补齐作业 1 验证并审核 6.6

- 用户明确暂停阶段推进，要求功能/统计分离、极端/非法输入、连续斐波那契、10 组随机、短任务夹长任务及记录审核。
- AI 核实现有分离已实现，补诊断通道与回归保障；10 组按每组 64 对完整 int32 抽样落地并保存种子、输入、预期与实际输出。
- 首轮新增断言错误见 B-005；判断依据为实际容量/峰值一致，修正测试而非处理器。Release/Debug 均通过。
- 审核发现远端仍私有、部分编号重复及状态过时，已修正文档可修部分；理解验收、公开访问、阶段 3/4 案例与效率比较仍明确未完成。
- 本轮用户提供测试设计方向，具体代码由 AI 实施；未虚构用户手写修改或理解验收。详见 engineering-audit.md、stage1-test-matrix.md。

## A-010：澄清用户的显式握手提议

- 用户询问进入作业 2 并提供 ready + nb_read 条件；AI 解释它是 FIFO 门控，不能代替 valid/ready/data 接口。
- 新增 stage2-interface-proposal.md，提出只替换指定边界，保留上游/下游 FIFO，并指出缓冲数量改变影响性能比较。
- 尚未修改模型或宣称用户已批准具体缓冲和周期实现；下一步是定时间线后编码。

## A-009：用户审查流水寄存器名称

- 用户认为 m_stage1/m_stage2 与 namespace stage1 容易混淆，提出更新顺序的解释。
- AI 采纳职责命名 m_magnitudeStage/m_orderedStage，补充命名空间及立即更新说明；时序算法未改变。
- 保留用户已有的头文件注释编辑；README 独立改动不纳入提交。

## A-008：按用户要求拆分四模块

- 2026-09-06 用户提出文件拆分及头文件注释图要求，AI 将声明与实现按模块迁移，更新 CMake 与当前阅读/调试文档。
- 保留此前业务/测试命名与算法；没有新增阶段功能；历史验证日志仍保留当时 modules.cpp 路径。
- 注释明确实际实现而非理想吞吐：外部 FIFO 延迟可见、完成沿不接收、结果交付阻塞、Output 测试节拍。
- 验证：Release/Debug 各 2/2 通过，后续读取以各模块头文件为入口。

## A-007：用户指定 Clean Code、测试命名和 VS Code 调试

- 2026-09-06 用户要求测试变量前缀、按外部 C++ CleanCode 规则整理、创建调试/运行 JSON。
- AI 读取指定文件；整理成员 m_、测试 m_test/test/TEST_、控制流大括号、声明拆行、命名常量、长函数拆分、版权/文档头及格式配置。
- 拆分 main 的参数解析、文件检查、统计输出和连线；保持函数短小。SystemC 固定 API 与 C++17 保留；不假装 C++11 可构建当前库。
- 命名过程中误把 Python subprocess.run 的 text 关键字参数改为 test_text，触发 TypeError；恢复第三方 API 参数，回归通过，详见 B-004。
- 检查发现另一项既有提交 719360f 提供了 IntelliSense/CMake Tools 配置；保留其配置意图与扩展推荐，新增独立 Debug 工作流。
- 未纳入 README 的既有用户改动。未宣称实际操作 VS Code GUI；GDB 命中和单步已验证。

## A-006：从四模块规格实现作业 1

- 2026-09-06 用户要求先定义模块与输入输出，再转化为代码。
- AI 先新增 stage1-design.md，随后实现 Parser/Transform/Compute/Output、顶层及测试。
- 实际用户修改：本轮尚无；接口沿用此前用户要求的方向/职责命名。
- AI 错误：手写 gcd(1024,17)=4；独立参考揭示错误，初步换行猜测无效后修正数值，见 B-003。
- 构建整理：针对当前 SystemC 3.0.1 去掉已废弃的 SC_HAS_PROCESS；启用模型侧 SC_ENABLE_ASSERTIONS 以保留 Release 下依赖模板检查，未屏蔽警告。最终增量构建无上述警告。
- 验证：2/2 CTest 通过；用户仍需逐模块理解及审阅，不能代替完成度确认。

## A-006b：SystemC 全景教学与硬件生成边界

原编号 A-006；2026-09-06 审核消除重复编号，内容保留。

- 日期：2026-09-06；用户要求面向有三年 C++ 经验的芯片初学者，完整解释 SystemC 与仿真代码如何变成电路。
- AI 说明：从 C++ 执行语义、事件调度、抽象层次和 GPU 性能建模，连接到可综合子集、HLS、RTL、逻辑综合和物理实现；明确架构模型不必直接综合。
- 证据和可复用结论见 L-004；代码示例为教学说明，未宣称已通过编译或 HLS。没有用户新的代码修改、理解验证或工具链选择。

## A-005：用户审查命名与工具链

- 日期：2026-09-06。
- 用户判断：TB 的 sc_out 命名 input 不合理，信号应该体现模块间方向；提出使用 LLVM 以支持潜在硬件生成。
- AI 修改：采纳命名意见，由 AI 执行重命名，见 D-005。原来的同名方案虽可运行，但教学可读性差。
- 工具链核对：解释仿真编译与 HLS/综合的区别，见 D-006；未将用户的疑问当作已经定案的编译器迁移。
- 验证：重命名后重新构建、CTest 1/1 通过、示例 PASS；没有新增同构测试。
- 归属：这是用户提出的实际设计审查意见，代码编辑由 AI 完成，不写成用户亲手修改代码。

## A-001：按学习节奏准备环境

- 补记日期：2026-09-06；实际搭建验证记录见 2026-09-05 工作日志。
- 用户要求：先分析对齐再计划；每天三小时；已了解六类接口，准备第一天环境。
- AI 行动：构建最小寄存器示例、测试入口和学习说明；未提前生成完整作业。
- 用户反馈：围绕端口绑定、进程、信号更新和握手逐步提问和推演。
- 用户代码修改：当前无已观察到的修改，不填造。
- 证据：[学习记录](learning-log.md)、[工作日志](work-log.md)。

## A-002：纠正两寄存器与两周期延迟的混淆

- 类型：AI 教学方案容易造成的时序歧义，不是已观察到的程序失败。
- 用户提出：两个寄存器“要 2 个周期才能输出”。
- AI 处理：明确本例 5 ns 采样、15 ns 输出，两个沿之间只有一个周期；必须先定义延迟的起止边界。
- 沉淀：D-004；本例只是信号语义实验，不能当作题目两周期流水的实现证据。
- 后续：实现前独立写时间线与验收标准。

## A-003：区分教学简化与题目硬性要求

- AI 建议：计算完成后等待结果交付，再回到空闲；给出 k+6 完成、k+7 交付、k+8 最早接收的示例。
- 限制：额外等待源于该简单状态机，题目只明确完成当沿不能同时接收。
- 处理：D-003 标记为教学提议，未擅自变成最终设计。
- 用户反馈：理解结果保持与输入流控的关系；未明确选择最终状态机。

## A-004：持续沉淀过程

- 日期：2026-09-06。
- 用户明确要求：顺序完成、持续设计调试验证记录、公开 Git、AI 方案修改和错误处理记录、阶段 3/4 主动缺陷案例。
- AI 实施：建立项目 AGENTS.md 和分类记录文档，补记有对话证据的结论。
- 重要限制：不把计划案例算成发现，不把 AI 建议算成用户设计，不补造历史测试和提交。

## A-007b：配置 VS Code 的 C++ 语法解析

原编号 A-007；2026-09-06 审核消除重复编号，内容保留。

- 日期：2026-09-06；用户要求让当前项目进入 VS Code 语法解析，并修复预编译诊断。
- 定位：仓库原先没有 `.vscode` 配置，CMake 也没有生成 `compile_commands.json`；编辑器无法获知 SystemC 源码头文件目录、`SC_ENABLE_ASSERTIONS` 和 C++17 等真实编译参数。
- AI 实施：启用 CMake 编译数据库；将 VS Code C/C++ 扩展指向该数据库，并固定为项目实际使用的 Strawberry GCC、Ninja 和构建目录；推荐 C/C++ 与 CMake Tools 扩展。
- 验证：重新配置后生成 `build/compile_commands.json`，其中 `modules.cpp` 命令包含 SystemC include、宏和 `-std=c++17`；两个 VS Code JSON 文件解析通过；构建无新增工作且 CTest 2/2 通过。
- 范围：这是编辑器与构建配置修复，没有修改 Stage 1 功能或时序设计；用户原有 `README.md` 工作树改动未触碰。

## A-008b：Python 终端 trace 可视化

原编号 A-008；2026-09-06 审核消除重复编号，内容保留。

- 2026-09-06，用户要求：图形化展示 SystemC 运行 trace，帮助理解过程、效率与优化。
- AI 实施：纯 Python 标准库彩色终端时间线、逐周期/事件导航、任务详情、FIFO 当前占用、播放、缩放与静态导出；只读 CSV，不改模型。
- 取舍：无需安装依赖；ASCII 图格配合中文说明。指标明确以最后事件为窗口边界；主机运行速度标为不可用，避免伪造效率数据。
- 实际修正：空键轮询原会使平移窗口回到游标，代码审查时修正并加入回归；小尺寸伪终端触发提示后放宽至 80×24 并复测。文档补丁一度因上下文不匹配未应用，修正后重新写入。
- 验证与限制见 [证据](evidence/stage1/trace-viewer-validation.md)；没有用户后续设计决策或手动修改可记录。原有 README.md 修改未包含。

## 2026-09-07：函数契约的实现选择

- 用户提出更具体的常量命名、所有函数入口断言抛异常和头文件注释优化。
- AI 纠正标准 assert 不抛异常且受 NDEBUG 影响；采用 requireCondition(condition, message)，逐类审阅函数，在真实约束处校验。
- 该接口名及纯查询函数无需附加断言属于 AI 实施取舍，不记为用户明确决定。未机械插入 assert(true)。
- 原 if/throw 重构保留异常类型和提示；新增零周期、坏流、空事件及状态一致性检查。头文件保留原时序契约并补充使用约束。
- Debug/Release 各 12/12 通过；范围与证据见 [详细记录](code-contracts.md)。没有用户实际手动改动或本轮测试失败可记录。

## 2026-09-08：显式标注 spec 外模型假设

- 用户要求：spec 之外的约定应在设计文档中明确表明。
- AI 实施：将既有零边界展开为 Model Assumption，列全三条 GCD 等式，明确非负幅值语义、b 为零时跳过取模及延迟公式，并同步最终设计入口；未改变代码。
- 文档补丁首次因日志上下文不匹配失败，修正后应用；未产生模型测试失败。

## 2026-09-09：统一全部项目头文件注释

- 用户明确指定 Doxygen 的 @brief、Interface 图、Protocol、Timing、Reset 格式；AI 已应用到全部 14 个项目头文件的主要说明，并将成员简短契约统一为块注释。
- 按实现填写 FIFO、握手、轮转和窗口协议；无复位端口写明构造初始化，辅助类型/工具写明时序和复位不适用；没有照搬示例中不存在的 req/valid/reset 接口。
- 保留零输入 Model Assumption、背压、参数寿命和异常约束；规范见 [coding-style.md](coding-style.md)。用户已有 README 修改未纳入。
- 验证：去除注释和空白后，14 个头文件与基线 d50bf5e 一致；五个必需说明项齐全。Release 构建退出 0，CTest 12/12 通过，无新失败。

## 2026-09-09：按 Model 职责分层重构

- 用户提出 Functional Behavior / Timing Behavior / Instrumentation（Trace、Statistics、Profiling、Debug）结构以提升可读性；AI 按职责抽取共享实现，保留各阶段模块边界，不新增硬件模块。
- 已实施：纯数值函数、共享延迟/周期工具、EventRecorder、模块统计结构、主机 Profiling 脚本；正式观察器不再使用 TestEventLog 命名。Debug 必需契约仍始终生效；全量 Instrumentation 开关未实现。
- 新增所有有效场景的 Trace 开关成对检查，填补上一轮识别的作业 2/3 证据缺口。Release/Debug 各 12/12，各 96 个成对场景；372 份重构前文件哈希一致。
- AI 曾漏改统计指针访问导致构建失败，已修正并保留原始诊断；详见 [分层验证](evidence/model-structure-validation.md)。用户提出结构方向，不记为用户亲自编写代码或已完成理解验收。

## 2026-09-09：作业 1 Transform 正向阅读

- 用户指出倒序更新不利于阅读；AI 改为当前/下一状态分离，正向执行取绝对值、排序、输出，末尾提交寄存器；方案细节属于 AI 选择。
- 保留 ready 反向传播与同拍补位，并维持原事件记录顺序。原写法未发现功能错误，本轮属于可读性重构。
- Release/Debug 阶段 1 回归各 1/1，重构前后 102 份文件哈希一致；见 [D-018](decisions.md#d-018作业-1-transform-正向计算下一拍状态) 与 [验证证据](evidence/stage1-forward-validation.md)。

## 2026-09-09：用户选择倒序实现

- 用户在讨论背压后明确认为倒序更好；AI 恢复作业 1 倒序原地更新并完善解释注释，未将两种等价写法描述为正确/错误之分。
- D-018 保留并标为被 D-019 替代；本次恢复代码和记录另行提交，不重写历史。验证见 [证据](evidence/stage1-forward-validation.md#2026-09-09用户选择恢复倒序)。

## 2026-09-09：澄清“副本”并建立作业 4 起点

- AI 最初把“副本”理解为工程备份并创建 backups/stages1-3-before-stage4；用户纠正为 src/stage4 内整合源码，保持功能作为初版。此前解释有误，按用户纠正执行；本轮检查时原备份路径已不存在，未推断删除者或原因。
- 用户批准以作业 3 最终版本及其依赖整合，独立构建后验证等价。AI 采用窗口版、扁平模块目录和本地 model/common 副本，明确不是作业 4 事件实现完成。
- 已落实并验证，见 [初始版本](stage4-initial.md)；未宣称用户亲自改源码或已完成理解验收。

## 2026-09-10：README 转向持续学习与架构实践

- 用户明确定位：项目作为学习 SystemC、仿真优化和架构设计的持续实践，展示成长与问题记录；README 简洁，参考 GoogleTest 的项目介绍与文档导航方式。
- AI 实施：重写 README，突出架构演进、模型分层、等价验证与真实问题复盘；保留快速开始与依赖信息，链接详细文档。延续用户已暂存的去除标题中“面试作业”的修改。
- 移除 README 已过时的“尚未实现作业 1”描述；事件驱动与提速明确为探索目标。历史阶段称谓与证据保留，不将此次定位调整视为历史需求或验收已完成。
- 参考：https://github.com/google/googletest ，本轮实际读取。只借鉴组织方式，未复制功能介绍。

## 2026-09-10：按用户纠正迁移 Parser

- 用户纠正 delta cycle 表述，确认保持原节拍；AI 按局部迁移实现 Parser 无时钟输入及 System 解绑，其余模块保留过渡时钟。
- 首次等价测试暴露同拍 CSV 全局行序假设过强，AI 保留失败证据并限定为流内顺序/时间严格相同；没有把测试问题包装成作业 4 处理器缺陷。见 [B-009](defects.md#b-009parser-事件迁移暴露日志比较过度约束)。
- 结果、限制与用户需求来源见 [Parser 设计](stage4-parser.md)；用户未被记为亲自编写代码或已完成理解验收。

## 2026-09-10：全系统去时钟与周期对照

- 用户要求完整去掉 clock 并比较 stage3；AI 实施最早事件期限调度、绝对计算完成时间和区间统计，原作业 1～3 不变。当前属于集中式事件模型，不是每个模块独立运行一个事件线程。
- 首次编辑脚本曾因 Windows 默认 GBK 读取 UTF-8 注释而失败，已显式启用 UTF-8 后继续；没有因此更改编码规范。
- 首轮测试全部独立功能/时序检查通过，但日志整文件比较被新增调度诊断行触发；保留失败日志并仅分离该诊断行。未把这种测试适配问题计为处理器缺陷。
- 最终 Release/Debug 各 15/15。百万周期等待仅 8 次事件批次，表明已跳过无变化区间；没有把批次数当成内核总唤醒数或声称主机实测提速。[设计和证据](stage4-events.md)。

## 2026-09-10：功能等价与主机仿真成本实测

- 用户要求比较四类指标并补充更能暴露问题的指标；AI 增加对称测量包装入口与可复现基准，不改模型实现。
- WMI 环境查询因权限失败，改用只读注册表和 Python 平台信息取得 CPU/系统；未将查询失败写成仿真故障。
- 140 次正式测量显示事件模型在长计算/空闲中受益，连续零延迟组略慢且四分位区间重叠，Trace 打开后两组均退化。AI 保留退化结果，没有以较少 delta 直接推断更快。
- 主动复现的 Trace 耗时/内存问题登记 B-011；同输入控制补测在主实验后完成，报告注明限制。原模型未改，优化未实施；[完整对照](model-comparison.md)。

## 2026-09-10：运行时检查命名调整

- 用户反馈 `requireCondition` 叫法不常见，希望改成接近 `assert` 的名称。
- AI 采用 `assertCondition`，并明确保留其与标准 `assert` 宏的语义差异：失败时抛异常，Release 仍执行。已统一源码、测试和当前规范；旧名称在历史记录中保留并由 D-024 标记为被替代。

## 2026-09-10：澄清 `BOUNDARY_COUNT` 的提问范围

- 用户纠正 AI 对“代码中的全部 const 变量”的理解：实际希望解释 `TaskStatistics` 内的 `BOUNDARY_COUNT` 常量。AI 未对模型实现作修改，改为依据 stage4 统计代码说明其事件边界、数组维度、完成条件和五组输出统计之间的关系。
- 前一轮的全局 const 清单不符合用户实际关注点；本轮保留这一纠正，不将其描述为用户已独立掌握统计实现。
- 用户进一步要求列举全部同类变量；AI 按 `constexpr`（包括 static/inline/函数局部）而非所有 `const` 变量组织说明，范围以 stage4 当前实现为准。


## 2026-09-10：处理用户提出的四项代码问题

- 用户提供单位硬编码、死字段、超长整数诊断和传递 include 依赖四条审查意见；AI 核实，区分潜在运行问题与维护风险，不将用户发现写为 AI 自行发现。
- 时间和参数问题在五个入口存在，统一修复；无用字段和依赖只调整 Stage4，保留旧轮转 Stage3 使用的计数器。先复现后修复，见 [四项修复](review-fixes.md)。
- 非 1 ns 测试只验证真实报表方法的单位换算，未宣称全系统任意周期配置已验收；默认功能时序和双配置 25/25 已验证。保留轮前已有的其他学习记录。

## 2026-09-10：设计四阶段指标与呈现方案

- 用户给出作业 1～4 的最低统计要求；AI 核对现有统计源码及既有证据，给出公式、单位、分母、表格/图形和实验控制条件，明确已有、可派生及待新增项。
- 源码核查发现现有忙周期与完成沿写出失败可重叠，接收沿也未必属于已有三类计数，因此不建议直接画百分比状态堆叠图；这属于口径分析，不是本轮复现缺陷。
- [D-025 与具体方案](metrics-presentation.md) 为 AI 建议，未修改代码或运行性能实验，未将建议写成用户已批准。

## 2026-09-10：实施第二轮规范修复（5～12 项）

- 用户确认 1～4 已修复后要求继续第二轮八项；AI 在 `9b34b47` 基线实施，范围仅 Stage4，逐项判断与验证见 [review-fixes](review-fixes.md)。
- 对首轮审查结论的一处更正：compute.hpp 顶部两个 @brief 实为 @file 与 @class 各自摘要，属 Doxygen 惯例而非重复，未改；实际仅移动错位至 advance() 的注释。
- 全部为结构性修改，用法字符串与统计输出逐字节不变（人工比对）。WSL 双配置 25/25、零警告；同日补测 Windows（Strawberry MinGW）双配置零警告、25/25，exe 输出一致，见 [review-fixes](review-fixes.md)。


## 2026-09-10：解释 Stage4 进程形式与调度选择

- 用户质疑 SC_METHOD 改为 data 敏感是否即可去掉 clock；AI 对照实际代码，确认 Dispatcher::route 仍是 SC_METHOD，并说明完整事件方案还须处理完成期限、背压和原输入节拍。
- 将集中调度明确为实现取舍而非 SystemC 限制；分散 SC_METHOD/事件方案为可行备选，未自动改写代码。学习结论见 [学习记录](learning-log.md#2026-09-10sc_method-与事件调度不是互斥选择)。
