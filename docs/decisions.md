# 设计决策

## D-005：连接与端口命名

- 日期：2026-09-06；来源：用户指出 tb.input 是 sc_out 容易误导，建议连接信号表达来源和去向。
- 选择：顶层信号用 tb_to_registers_data / registers_to_tb_data；模型端口用 data_in / data_out；TB 端口用 stimulus_out / result_in。
- 理由：顶层表达连接拓扑，端口表达相对于本模块的方向和职责；模块端口不绑定具体外部实例，便于复用。
- 状态：已实施并验证，现有 CTest 1/1 通过；功能和时间线不变，波形信号名同步调整。
- 替代：旧 input/output 同名命名，仅作为历史讨论保留。

## D-006：仿真编译与硬件综合的边界

- 日期：2026-09-06；来源：用户提出应使用 LLVM，理由为后续可能生成硬件电路。
- 核对：Clang 是 LLVM 项目的 C++ 前端；普通 Clang/GCC 编译 SystemC 生成仿真程序，不自动生成电路。硬件路径需要支持 SystemC 可综合子集的综合/HLS 工具及其约束。
- AI 建议：本题仅要求架构性能仿真，保留已验证 GCC；如用户另有工具链偏好，可独立迁移 Clang 并重建库与模型。
- 状态：编译器尚未切换；保留 GCC 是本轮 AI 建议，不记录为用户已同意。
- 官方依据：https://clang.llvm.org/ 、https://www.accellera.org/activities/working-groups/systemc-synthesis 、https://www.accellera.org/resources/videos/systemc-tutorial-2019 。

2026-09-06 初始化；下列既有内容为对话与文件的历史补记，未伪造历史提交。

## D-001：构建环境

- 来源：用户要求在当前目录准备编译运行测试环境；AI 提出复用已有工具。
- 选择：Windows MinGW GCC 13.2、C++17、CMake/Ninja；官方 SystemC 3.0.1 源码同工程构建。
- 理由：复用现有环境，保证依赖和模型使用同一编译器及语言标准。
- 代价：Windows 运行需可找到 MinGW 运行库；首次需下载并编译依赖。
- 状态：已实施，基础构建运行已验证。不是完整作业或跨平台验证。
- 证据：[README](../README.md)、[工作日志](work-log.md)。

## D-002：阶段和时间分配

- 来源：题目要求顺序完成并优先已完成阶段质量；用户每天三小时、一周约 21 小时。
- AI 建议：优先形成可靠的前三阶段，按进度尝试第四阶段。
- 状态：题目顺序和质量原则由用户再次明确要求遵守；“前三阶段作为时间预算优先级”是计划建议，不是降低完整作业范围。
- 验收：四阶段分别跟踪，未实现不得记为完成。

## D-003：计算状态与结果保持

- 来源：对话教学中的 AI 方案。
- 提议：空闲、计算中、结果待交付；结果未交付时保持输出数据和 valid，简单实现不接新任务。
- 理由：避免尚未交付的结果被覆盖。
- 代价：可能引入额外接收间隔；加独立有限结果缓冲是后续可比较选项。
- 状态：教学方案，尚未实现，也未确定为最终架构。
- 硬性约束：完成当前任务的上升沿不能同时接收下一任务。
- 注意：教学示例中的第 6 拍完成、第 7 拍交付、第 8 拍接收，不是题目规定的唯一时序。

## D-004：延迟定义与待决事项

- 来源：两寄存器示例及题目两周期变换要求的对齐。
- 已知：示例在 5 ns 首次采样，在 15 ns 更新输出；两次采样沿之间是一个完整周期。
- 约束：不能直接用“两个寄存器”证明满足题目“两周期延迟”。
- 待决定：流水入口接收沿、出口可用沿、下游接收沿的定义；计算接收 k、完成 k+L 的约定；零次取余的边界。
- 同时待决定：有限缓冲容量、同拍入出规则、输出速率、统计区间、INT32_MIN 内部表示。
- 状态：待定；进入作业 1 实现前应写出时间线并形成测试预期。
