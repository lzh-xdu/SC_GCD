# 设计决策

## D-019：用户选择恢复作业 1 倒序原地更新

- 2026-09-09，讨论背压传播与临时状态后，用户明确判断倒序写法更适合当前实现；恢复输出、排序、接收的顺序，注释说明空槽反向传播、同拍补位和防跨级。
- 状态：已实施、已验证，替代 D-018 的正向实现选择；原正向方案与等价证据保留。两者功能时序等价，本决定不宣称倒序具有已测量的主机性能优势。
- 验证见 [恢复倒序回归](../evidence/stage1-forward-validation.md#2026-09-09用户选择恢复倒序)。

## D-018：作业 1 Transform 正向计算下一拍状态

- 2026-09-09，用户指出倒序实现不利于阅读；AI 采用当前状态/下一状态分离，按取绝对值、排序、输出顺序计算，最后统一提交两个 optional。
- 先由输出 FIFO 空位向上游推导可推进条件，保留同拍排空补位。排序和输出只读取旧成员，防止新任务一拍跨两级；阻塞时保留原槽。
- 代价：增加两个临时 optional 和显式可推进条件；它们是下一状态计算值，不是额外硬件缓冲。SC_METHOD 不让出执行权，当前绑定 sc_fifo 的 num_free 与 nb_write 一致；写入结果仍用运行时契约检查。
- 状态：已实施、已验证；仅替代 D-011 中作业 1 的倒序更新实现，原延迟/容量/吞吐约定保持。用户未指定具体实现，也未完成理解验收。
- 验证：见 [正向重构证据](../evidence/stage1-forward-validation.md)。
- 后续状态：同日用户经背压讨论选择恢复倒序；本实现选择已被 D-019 替代，以上保留历史记录。

## D-017：统一观察边界与关键阻塞统计

- 2026-09-07，用户要求落实优化审查第一项并精简最终设计；AI实施，未替用户确认理解。
- 五事件 P/A/C/E/O 划分计算前、计算、结果待写、后续交付，端到端 O−P；观察不计入硬件缓冲，不改变调度；所有模型共用同一实现。
- 增加空闲未接收、无窗口信用但有 ready；保留已有阻塞口径，明确重叠及子集关系。
- 代价：精确P95保留O(N)样本并排序；后续事件驱动效率对照须统一统计配置。已实施且独立验证，见 [统计口径](task-statistics.md) 与 [证据](../evidence/task-statistics/validation.md)。

## D-016：用户改为择闲分配及预留滑动窗口

- 用户明确取代默认的固定轮转偏好：两空闲随机、一个空闲就选它，结果提前缓存并按编号输出。
- 新目标stage3_window_gcd落实该方案，旧stage3_gcd保留作对照。编号和三态Compute原有实现复用。
- AI细化：任务派发时预留[base,base+W)槽，窗口满阻止派发而不阻止最早已预留结果返回，避免草案“满时全停收”的潜在死锁。风险为推理，没有虚构已观察失败。
- 固定容量环形窗口，每拍最多收/发一项，先发后收；默认W=8，随机状态按成功双空闲选择推进，默认seed=1可复现。
- 已实施/验证；基线先测、完整收益与退化及额外资源见stage3-window.md。R=6旧版与W=8新版声明载荷相等，仅作容量对照，不称等面积。

## D-012：作业 2 接口及缓冲落地

- 用户授权实现，AI选择排序槽直接承担valid/data保持，移除该边界外部FIFO；完成沿不接下一项，遵循题目。
- 已实现并独立提交75c76f5；协议/时序测试通过。资源改变明确记录，不把所有周期变化归因于握手。

## D-013：严格轮转分配和顺序合并

- 用户明确选择简单顺序。具体实现为成功握手后0/1轮换，被选端忙则等待，不择闲跳过。
- 两路独立结果FIFO，Collector按next_id%2读取，先编号未到则保持后编号；无通用重排表，控制简单且容量有限。
- 代价为负载不均/保序等待，统计公开。已实现及验证；不改成动态调度。

## D-014：以实际压力结果调整结果FIFO默认容量

- AI初始最小R=1被B-006场景暴露额外停顿；实际R=2缩短663→648周期，新增2槽；R=4未继续缩短。
- 默认改2、保留参数，测试记录三种配置。不宣称2是所有负载最优值。

## D-015：成本用可追溯资源代理表示

- 用户提出Compute增加的成本/面积/功耗。当前实现报告单元数、内部槽、FIFO槽及声明载荷bit，并比较每Compute吞吐的相对效率。
- 不把C++ sizeof当硅面积，不伪造功率/能耗；载荷不含控制/计算电路成本。真实面积与功耗需要具体实现及标定。
- 已实施；定义与单/双数值见stage3-run.md。

## D-011：流水成员按职责命名

- 2026-09-06 用户指出流水级编号难以理解且易与 namespace 混淆。
- m_stage1 → m_magnitudeStage，m_stage2 → m_orderedStage；stage1 命名空间仍表示作业版本，不表示流水级。
- 先发旧排序级、再移动旧绝对值级、最后接收输入的行为保持。成员是立即更新的 optional，不是 sc_signal。
- 2026-09-09：上条倒序实现对作业 1 已被 D-018 替代；此处保留历史结论，作业 2 未改动。

## D-010：每模块独立文件与接口图

- 2026-09-06 用户要求每个模块单独头文件和 cpp，头文件注释说明输入输出、触发、缓存、时钟周期。
- Parser/Transform/Compute/Output 分别移入 parser/transform/compute/output 的 .hpp/.cpp；共享类型、打印与测试事件观察器放 types.hpp/.cpp。
- 绝对值 helper 只在 transform.cpp；GCD/延迟 helper 只在 compute.cpp；main 显式包含四个模块。
- 图中区分外部 FIFO（默认容量 2）、内部寄存器/任务位置；列出正常节拍、阻塞与零延迟边界。
- 状态：已实现，Release/Debug 各 2/2 CTest 通过；算法、输入输出格式和时序未改变。

## D-008：代码风格与测试变量前缀

- 来源：2026-09-06 用户明确要求，规则来源和项目适配见 coding-style.md。
- 选择：成员 m_ + camelCase，测试成员 m_test、测试局部/参数 test、常量 TEST_；数据/资源性能计数不标为测试。
- D-005 的方向/职责原则保持，接口 C++ 名称变为 m_dataIn/m_dataOut、m_tasksIn/m_tasksOut 等；旧命名是历史，不是当前代码引用。
- 状态：已实施；Release/Debug 回归和基本统计对照通过，数值和周期不变。

## D-009：VS Code Debug 工作流

- 来源：用户要求创建 JSON 配置用于运行和调试。
- 使用当前 GCC 配套 GDB，build/debug 独立 Debug 构建；不混用 Release 产物；设置运行库路径和启动参数。
- 保留已有 CMake Tools/IntelliSense 设置；新增任务和启动配置，见 vscode-debug.md。
- 状态：两模式测试通过，GDB 命中 Parser::tick/Testbench::run、读取变量、next 和继续退出均成功；VS Code GUI 未实际点击验证。

## D-007：作业 1 四模块接口与时序

- 2026-09-06 用户要求先写四模块输入输出规格，再转为代码。
- 本轮 AI 方案与实现见 stage1-design.md：有限 sc_fifo、两条变换寄存器、单实例计算、结果文件；显式握手留到作业 2。
- D-004 中本阶段细节已在规格中选定：变换 k 接收/k+2 写出；FIFO 写后下一沿可读；计算 k 接收/k+L 完成；零取余 L=0；同拍完成不接新任务；默认容量 2；输出每拍最多一条。
- 对 D-003 的细化：完成沿若 FIFO 有空位即可写出，下一沿才能接新任务；不是固定额外等待第 7/8 拍。
- 类型：原始 int32，绝对值及结果 uint64；禁止原类型直接对 INT32_MIN 取负。
- 状态：代码与测试已实现，基础/随机/背压/时序检查通过；待用户审阅。不能记成所有具体选择已经获得用户逐项批准。
- 代价：不使用 FIFO 同沿满读后立即写的旁路，容量 1 存在额外等待；换资源规则需重建时序预期。

## D-005：连接与端口命名

- 日期：2026-09-06；来源：用户指出 tb.input 是 sc_out 容易误导，建议连接信号表达来源和去向。
- 选择：顶层信号用 tb_to_registers_data / registers_to_tb_data；模型端口用 data_in / data_out；TB 端口用 stimulus_out / result_in。
- 理由：顶层表达连接拓扑，端口表达相对于本模块的方向和职责；模块端口不绑定具体外部实例，便于复用。
- 状态：已实施并验证，现有 CTest 1/1 通过；功能和时间线不变，波形信号名同步调整。
- 替代：旧 input/output 同名命名，仅作为历史讨论保留。

## D-006：仿真编译与硬件综合的边界

- 日期：2026-09-06；来源：用户提出应使用 LLVM，理由为后续可能生成硬件电路。
- 核对：Clang 是 LLVM 项目的 C++ 前端；普通 Clang/GCC 编译 SystemC 生成仿真程序，不自动生成电路。硬件路径需要支持 SystemC 可综合子集的综合/HLS 工具及其约束。
- AI 建议：本题仅要求架构性能仿真，保留已验证 GCC；如用户另有工具链偏好，可独立迁移 Clang 并重建库与模型。
- 状态：编译器尚未切换；保留 GCC 是本轮 AI 建议，不记录为用户已同意。
- 官方依据：https://clang.llvm.org/ 、https://www.accellera.org/activities/working-groups/systemc-synthesis 、https://www.accellera.org/resources/videos/systemc-tutorial-2019 。

2026-09-06 初始化；下列既有内容为对话与文件的历史补记，未伪造历史提交。

## D-001：构建环境

- 来源：用户要求在当前目录准备编译运行测试环境；AI 提出复用已有工具。
- 选择：Windows MinGW GCC 13.2、C++17、CMake/Ninja；官方 SystemC 3.0.1 源码同工程构建。
- 理由：复用现有环境，保证依赖和模型使用同一编译器及语言标准。
- 代价：Windows 运行需可找到 MinGW 运行库；首次需下载并编译依赖。
- 状态：已实施，基础构建运行已验证。不是完整作业或跨平台验证。
- 证据：[README](../../README.md)、[工作日志](../work-log.md)。

## D-002：阶段和时间分配

- 来源：题目要求顺序完成并优先已完成阶段质量；用户每天三小时、一周约 21 小时。
- AI 建议：优先形成可靠的前三阶段，按进度尝试第四阶段。
- 状态：题目顺序和质量原则由用户再次明确要求遵守；“前三阶段作为时间预算优先级”是计划建议，不是降低完整作业范围。
- 验收：四阶段分别跟踪，未实现不得记为完成。

## D-003：计算状态与结果保持

- 来源：对话教学中的 AI 方案。
- 提议：空闲、计算中、结果待交付；结果未交付时保持输出数据和 valid，简单实现不接新任务。
- 理由：避免尚未交付的结果被覆盖。
- 代价：可能引入额外接收间隔；加独立有限结果缓冲是后续可比较选项。
- 状态：教学方案，尚未实现，也未确定为最终架构。
- 硬性约束：完成当前任务的上升沿不能同时接收下一任务。
- 注意：教学示例中的第 6 拍完成、第 7 拍交付、第 8 拍接收，不是题目规定的唯一时序。

## D-004：延迟定义与待决事项

- 来源：两寄存器示例及题目两周期变换要求的对齐。
- 已知：示例在 5 ns 首次采样，在 15 ns 更新输出；两次采样沿之间是一个完整周期。
- 约束：不能直接用“两个寄存器”证明满足题目“两周期延迟”。
- 待决定：流水入口接收沿、出口可用沿、下游接收沿的定义；计算接收 k、完成 k+L 的约定；零次取余的边界。
- 同时待决定：有限缓冲容量、同拍入出规则、输出速率、统计区间、INT32_MIN 内部表示。
- 状态：待定；进入作业 1 实现前应写出时间线并形成测试预期。

## D-020：作业 4 先建立功能不变的独立源码起点

- 2026-09-09，用户要求并批准：先创建 stage4 整合旧阶段所需源码，再逐步改造事件模型。
- AI 实施选择：以 stage3_window 为拓扑，复制必要模块及 model/common，统一阶段命名空间，独立链接 SystemC。代价是暂时重复实现；收益是后续改造不改变旧阶段基线。
- 状态：初始整合已实施、已验证；不包含事件模型、提速或真实缺陷案例验收。对应关系及等价证据见 [作业 4 初始版本](stage4-initial.md)。

## D-021：Parser 保持原输入节拍并以空间事件解除阻塞

- 2026-09-10，用户明确纠正“每个 delta 输入一对”，选择保持原输入节拍。AI 实施首次 1 ns、后续最快每 1 ns；满 FIFO 等待读取事件，按下一输入边界恢复。
- 删除 Parser 时钟端口和顶层绑定；其他模块与观察器保留过渡时钟。输入边界后一次 delta 等待维护原 FIFO 可见性，不推进物理时间。
- 状态：Parser 局部迁移已实现并验证，完整系统迁移待完成。时序、代价及测试见 [Parser 设计](stage4-parser.md)。

## D-022：全系统采用集中式事件调度与区间统计

- 2026-09-10，用户要求本次移除所有时钟并比较作业 3 的周期数。已移除 stage4 所有时钟对象/端口/边沿敏感；旧阶段保持原样。
- AI 选择集中式最早期限调度，模块保留功能转移；Compute 使用绝对完成时间，Parser 生产模式由 System 管理输入期限。该选择扩展 D-021 的局部迁移，独立 Parser 模式保留供测试。
- 空间、输入、流水或输出不能推进时不安排固定周期唤醒；区间累计统计，Trace 开启时仅在记录层补齐历史格式控制行。代价是详细 Trace 的内存/输出成本，以及当前拓扑三个 delta 稳定阶段需要随组合结构变化复核。
- 已实现并验证：Release/Debug 各 15/15，31+4 场景结果/周期/统计一致。方案与限制见 [事件模型](stage4-events.md)；不记为正式主机性能或全部作业 4 验收完成。

## D-023：用对称包装入口测量模型执行效率与主机开销

- 2026-09-10，用户要求比较功能、系统时间/性能、仿真效率、开销及补充指标。
- AI 采用独立 Windows profiling 目标，原模型入口完整复用，不改生产源码；记录模型入口/整个进程耗时、CPU、峰值工作集/commit、SystemC delta 轮次和事件批次。1 次预热、7 次正式测量、两模型顺序逐轮交替，避免同时运行模型争抢资源。
- 功能由 math.gcd 独立校验，包装与普通二进制结果/统计/Trace 哈希一致；主实验 8 配置，补测 2 个同输入 Trace-off 控制。保留所有重复样本、中位数、四分位数及最值，不用一次最快结果。
- 状态：本机测量已完成。限制：不是纯 sc_start 耗时，CPU 计时粒度有限、未控制频率和背景活动，未测总进程唤醒/事件通知次数。Trace 问题 B-011 的优化仅为建议，未实施。见 [实测报告](model-comparison.md)。

## D-024：运行时契约检查采用 assertCondition 命名

- 2026-09-10，用户指出 `requireCondition` 不够常见，希望名称接近 `assert`。选择 `assertCondition(condition, message)`，统一替换各阶段、测试和当前规范中的调用。
- 该函数仍在条件为假时抛出指定异常，并在 Debug/Release 都生效；它不是标准 `assert` 宏，因而不受 `NDEBUG` 移除。保留 `Condition` 后缀以避免调用者误以为会终止进程或编译期消除。
- 状态：已实施并验证：Release 完整构建成功，`ctest --output-on-failure` 15/15 通过（含 `contract_checks` 的 6 项）。`requireCondition` 仅保留在历史日志中，作为被替代的旧命名记录。

## D-025：四阶段统计与呈现口径（提议）

- 2026-09-10，用户要求设计四个作业的具体统计指标与呈现方式；AI 按统一全程分母、已有/派生/新增三种覆盖状态设计表格、图形和对照实验。
- 方案区分重叠阻塞与整机性能损失、架构升级与纯并行收益、模拟硬件时间与主机耗时；不把现有忙/等待/空闲计数堆叠成互斥状态图。
- 状态：文档提议，未修改模型、未新增实验、未记为用户批准。详见 [统计与呈现方案](metrics-presentation.md)。

## D-026：SystemC Web 查看器的数据来源与渲染栈（AI 提议，待用户审阅）

- 2026-09-10，用户要求"设计一个 Python 脚本，以 SystemC 模块代码为输入、用前端网页动画展示模块链接和运行周期"；AI 给出设计并实现首版。
- 选项：周期动画数据来源（a）真实事件 CSV，（b）内置抽象 delta-cycle 伪仿真器，（c）VCD 波形。选择（a）+ 预留（c）：复用 `trace_tui.py` 的 F/T/Q/C/B/O 区间语义，动画展示的是实际仿真时序，不虚构时序；代价是依赖已有 trace、无 trace 则无周期动画。
- 选项：结构解析（a）正则轻量解析器，（b）libclang AST。选择（a）先落地并适配项目现有 `SC_MODULE`+`sc_fifo_in/out` 风格；（b）作为后续精确解析备选，现仅记局限。
- 选项：前端（a）CDN 图库（Cytoscape/d3），（b）自包含原生 JS+SVG。选择（b）：离线可打开、与项目"零第三方依赖"惯例一致。
- 状态：已实施首版（scripts/scviz*、docs/web-viewer.md），未记为用户批准；用户尚未确认 render 方向与 libclang/VCD 扩展优先级。
